def calc_power(a, b):
    return a**b
base = eval(input("Enter the number for the base: "))
exponent = eval(input("Enter the number for the exponent: "))
result = calc_power(base,exponent)
print("cadena de texto" + result)